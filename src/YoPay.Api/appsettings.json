{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5433;Database=yopay;Username=yopay;Password=yopay"
  },
  "Security": {
    "_comment": "Set via environment or Docker secret. Security__ActiveKeyId and Security__Keys__<id>. Generate: openssl rand -base64 32",
    "ActiveKeyId": "",
    "Keys": {}
  },
  "Checkout": {
    "_comment": "Where the hosted checkout page is reachable. Override per environment; the API only ever concatenates an invoice id onto it.",
    "BaseUrl": "http://localhost:5082"
  }
}

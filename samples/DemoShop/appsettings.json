{
  "Logging": { "LogLevel": { "Default": "Information", "Microsoft.AspNetCore": "Warning" } },
  "AllowedHosts": "*",
  "YoPay": {
    "_comment": "KeyId and Secret come from the YoPay dashboard's Issue API key button. WebhookSecret comes from registering an endpoint - it is shown once. Put the real values in user secrets, not here.",
    "BaseUrl": "http://localhost:5080",
    "KeyId": "",
    "Secret": "",
    "WebhookSecret": ""
  }
}
